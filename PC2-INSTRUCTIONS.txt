PC2 ProfitTrader -- Post-Fix Instructions (March 14, 2026 - Updated)
=====================================================================

After today's fixes on claude/review-repo-docs-LmWVI, PC2 needs these steps.
Run these on ProfitTrader (192.168.1.116).

Read this file, then execute each step in order.


STEP 1 -- SET PYTHONUTF8=1 PERMANENTLY (skip if already done)

  [Environment]::SetEnvironmentVariable("PYTHONUTF8", "1", "User")


STEP 2 -- PULL LATEST CODE

  cd C:\Users\ProfitTrader\elite-trading-system
  git fetch origin claude/review-repo-docs-LmWVI
  git checkout claude/review-repo-docs-LmWVI
  git pull origin claude/review-repo-docs-LmWVI

Verify HEAD is at 241d91c or later:
  git log --oneline -7

You should see (newest first):
  241d91c fix: retry DuckDB init on transient lock after crash recovery
  b02d503 fix: thread-pool Kelly sizer and order executor DuckDB calls
  5c5ed47 chore: add PYTHONUTF8=1 to PC1 startup scripts
  494926d chore: add PYTHONUTF8=1 to PC2 startup scripts
  dc11870 fix: thread blocking startup calls
  6672d93 fix: thread DuckDB health_check() calls
  701fd4f fix: harden startup to prevent port bind races


STEP 3 -- FIX .ENV BAD BYTE (if PC2 has its own backend/.env)

  $envPath = "C:\Users\ProfitTrader\elite-trading-system\backend\.env"
  if (Test-Path $envPath) {
    $bytes = [System.IO.File]::ReadAllBytes($envPath)
    for ($i=0; $i -lt $bytes.Length; $i++) { if ($bytes[$i] -eq 0x9D) { $bytes[$i] = 0x20 } }
    [System.IO.File]::WriteAllBytes($envPath, $bytes)
    Write-Host ".env fixed"
  } else {
    Write-Host "No .env on PC2 -- OK if using start-profittrader.ps1 (brain only)"
  }


STEP 4 -- CLEAN STALE FILES (if PC2 previously crashed)

  Remove-Item "C:\Users\ProfitTrader\elite-trading-system\.embodier.pid" -EA SilentlyContinue
  Remove-Item "C:\Users\ProfitTrader\elite-trading-system\backend\data\*.wal" -EA SilentlyContinue
  Stop-Process -Name python -Force -EA SilentlyContinue
  Start-Sleep 2


STEP 5 -- CHOOSE LAUNCHER

Option A -- Brain-only (production, recommended):
  cd C:\Users\ProfitTrader\elite-trading-system
  .\start-profittrader.ps1

  This starts Ollama + brain_service gRPC on port 50051.
  PC1 connects via BRAIN_HOST=192.168.1.116.

Option B -- Full stack (development/testing):
  cd C:\Users\ProfitTrader\elite-trading-system
  .\start-pc2.ps1

  This starts backend (8001) + frontend (3000) + brain_service (50051).


STEP 6 -- VERIFY BRAIN SERVICE

From PC2, verify Ollama is responding:
  Invoke-WebRequest "http://localhost:11434/api/tags" -UseBasicParsing

From PC1 (ESPENMAIN), test gRPC connectivity:
  Invoke-WebRequest "http://localhost:8000/api/v1/health" -UseBasicParsing |
    ConvertFrom-Json | Select-Object -ExpandProperty brain

Should show: connected=True, host=192.168.1.116, port=50051


WHAT NOT TO DO ON PC2:
- Do NOT run start-embodier.ps1 -- that is PC1-only
- Do NOT run scripts/run_full_stack_24_7.ps1 -- that is PC1-only
- Do NOT trade from PC2 -- orders only execute on PC1 (Key 1)
- Do NOT change ALPACA_API_KEY on PC2 -- it uses Key 2 (discovery only)


WHAT CHANGED TODAY (all commits on claude/review-repo-docs-LmWVI):
- Kelly sizer audit trail offloaded to thread pool (was blocking event loop)
- Order executor sizer.calculate() and ATR fetch wrapped in asyncio.to_thread()
- DuckDB init retries once after 3s on transient lock (WAL recovery after crash)
- Health endpoint _messagebus_queue_depth() made async
- asyncio.to_thread() for all DuckDB health_check() and init_schema() calls
- PID-file process lock prevents duplicate backend instances
- DUCKDB_PATH env override lets each PC use a different database file
- PYTHONUTF8=1 added to all startup scripts (PC1 and PC2)
- Port bind race hardening prevents respawn storms
