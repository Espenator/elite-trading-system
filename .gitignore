# Elite Trading System / Embodier Trader - Root

# ── Launcher state (chosen ports when preferred was busy) ──
.embodier-ports.json

# ── Environment (AUDIT FIX Task 16: catch ALL .env variants) ──
.env*
!.env.example
.github-token
backend/.env*
!backend/.env.example
APIFINAL*.env
APIFINAL*.xlsx
*-backend.env
*-frontend*.env
*-COMPLETE.env

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
.venv/
*.egg-info/
dist/
build/

# ── Database files ──
*.db
*.sqlite
*.sqlite3
finviz_stocks.db
*.duckdb
*.duckdb.wal

# ── ML Model artifacts ──
models/artifacts/
*.pkl
*.joblib
*.h5
*.pt
*.pth

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──
.vscode/
.idea/
.cursor/
*.swp
*.swo

# ── Logs ──
*.log
logs/

# ── Node/Frontend ──
node_modules/
frontend-v2/dist/

# ── Coverage & Testing ──
htmlcov/
.coverage
.pytest_cache/
.tox/

# ── Runtime data (root-level only, not backend/app/data/) ──
/data/

# ── Auto-updater hash files ──
.deps-hash
.build-hash

# ── Runtime / PID ──
*.pid
.embodier.pid

# ── Test output (temporary) ──
test_results*.txt

# ── Misc ──
*.bak
*.tmp
.claude/
*_test_result.json
