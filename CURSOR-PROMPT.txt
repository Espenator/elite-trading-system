Embodier Trader PC1 (ESPENMAIN) — Quick Fix Checklist
Branch: claude/review-repo-docs-LmWVI
Repo: C:\Users\Espen\elite-trading-system

The backend threading fixes are already merged. You need to do these 5 things:

1. SET PYTHONUTF8 PERMANENTLY (one command):
   [Environment]::SetEnvironmentVariable("PYTHONUTF8", "1", "User")

2. FIX .ENV BAD BYTE (run this PowerShell block):
   $p = "C:\Users\Espen\elite-trading-system\backend\.env"
   $b = [System.IO.File]::ReadAllBytes($p)
   for ($i=0; $i -lt $b.Length; $i++) { if ($b[$i] -eq 0x9D) { $b[$i] = 0x20 } }
   [System.IO.File]::WriteAllBytes($p, $b)

3. PULL LATEST CODE:
   cd C:\Users\Espen\elite-trading-system
   git pull origin claude/review-repo-docs-LmWVI

4. DISABLE SCHEDULED TASK (prevents respawn storm):
   Disable-ScheduledTask -TaskName "EmbodierTrader-24-7" -ErrorAction SilentlyContinue

5. CHECK WS CONNECTION — if frontend says "WS not connected":
   - Open browser DevTools (F12) > Console tab
   - Look for WebSocket errors
   - The backend WS endpoint is ws://localhost:8000/ws?token=YOUR_API_AUTH_TOKEN
   - Check backend/.env has API_AUTH_TOKEN set
   - The frontend reads it from the /api/v1/settings endpoint

VERIFY: Open http://localhost:8000/api/v1/health in browser. Should respond in under 2 seconds.

DO NOT: commit .env, use yfinance, add mock data, run start-embodier.ps1 while checking (supervisor will respawn)
