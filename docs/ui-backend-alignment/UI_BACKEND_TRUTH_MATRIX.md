# UI / Backend Truth Matrix

| Page | UI element | Current frontend behavior | Intended mockup behavior | Actual backend source | Status | Required action | Owner area |
|---|---|---|---|---|---|---|---|
| Shared layout | Bottom status ticker | Previously hardcoded market prices/regime; now shows real market indices or zero-state | Dense live market strip with truthful regime badge | `Layout.jsx` + `/api/v1/market/indices` + `/api/v1/openclaw/regime` | real / fixed | Keep zero-state when no snapshot exists | frontend |
| Shared layout | Bottom status health | Footer now uses real websocket/API/agent inputs when provided | Live operator footer | `useCNS`, `/api/v1/status`, `/api/v1/system` | partial | Add shared freshness/uptime/load only when real sources exist | frontend |
| Dashboard | Emergency stop control | Decorative / toast-style control | Real emergency control or absent | `/api/v1/orders/emergency-stop` | fake | Wire or remove | frontend |
| Dashboard | KPI / intelligence cards | Mix of real API polling and some optimistic chrome | Dense truthful intelligence console | `market.py`, `portfolio.py`, `performance.py`, `signals.py`, `risk.py`, `agents.py`, `sentiment.py` | partial | Remove remaining optimistic status text; add freshness and errors | frontend |
| Agent Command Center | Agent summary header | Now parses `{agents}` correctly and renders real counts/metrics | Real fleet control header | `/api/v1/agents`, `/api/v1/status`, `/api/v1/system/gpu` | real / fixed | Keep values backend-derived only | frontend |
| Agent Command Center | Emergency stop button | Now sends real emergency-stop request | Real destructive admin control | `/api/v1/orders/emergency-stop` | real / fixed | Preserve auth/error messaging | frontend |
| Agent Command Center | Footer strip | Now shows websocket/API/topics/alerts/mode/refresh from real sources | Dense truthful operator footer | `/api/v1/status`, `/api/v1/system`, `/api/v1/system/event-bus/status`, `/api/v1/agents/alerts` | real / fixed | Reuse pattern elsewhere | frontend |
| Agent CC / Swarm Overview | ELO leaderboard | Now parses `{leaderboard}` and shows empty state if none | Real operator ranking table | `/api/v1/agents/elo-leaderboard` | real / fixed | None | frontend |
| Agent CC / Blackboard | MessageBus topic table | Now renders backend topic list instead of static topic array | Real bus visibility | `/api/v1/system/event-bus/status` | real / fixed | Add freshness indicator | frontend |
| Agent CC / Blackboard | HITL buffer | Now renders backend queue and real approve/reject actions | Real human approval queue | `/api/v1/agents/hitl/buffer`, `/api/v1/agents/hitl/{id}/{action}` | real / fixed | Add defer if desired | frontend |
| Agent CC / Conference | DAG visualization | Uses backend DAG config but only latest conference, not rich history | Multi-stage conference visibility | `/api/v1/council/status`, `/api/v1/agents/conference` | partial | Add backend history endpoint before richer UI | both |
| Agent CC / ML Ops | Registry table | Now derives rows from real registry payload and shows empty state | Real model registry + training view | `/api/v1/ml-brain/registry/status`, `/api/v1/agents/drift` | partial | Expand once backend exposes richer run/job telemetry | both |
| Agent CC / Logs | Log stream | Now uses backend log payload or zero-state, no client demo list | Real operator telemetry | `/api/v1/logs` | partial | Backend route is still stub-like; replace backend sample logs | both |
| Sentiment | Heatmap / source bars | Static presentation lists still shape the page | Real symbol/source sentiment matrix | `/api/v1/sentiment/*` | partial | Make all symbol/source lists data-driven | frontend |
| Sentiment | Discover controls | Discover action exists but source attribution/freshness is weak | Operator-grade visibility | `/api/v1/sentiment/discover`, `/api/v1/sentiment/summary`, websocket sentiment channel | partial | Add timestamps and attribution | frontend |
| Data Sources | Source inventory | Hardcoded source catalog and metrics | Real feed-health manager | `/api/v1/data-sources/` | fake / partial | Render only backend-reported sources and metrics | frontend |
| Data Sources | Health badges / websocket badge | Decorative labels imply live status | Truthful transport/source status | `/api/v1/data-sources/`, websocket state if available | fake | Bind or remove | frontend |
| Signal Intelligence V3 | Scanner toggles/states | Hardcoded scanners, metrics, green states | Real multi-source operator console | many page-level API sources + websocket | fake / partial | Replace static state arrays with backend-driven or blocked UI | frontend |
| Signal Intelligence V3 | Endpoint registry panel | Decorative endpoint list | Real service health map | `/api/v1/status`, `/api/v1/system`, service-specific routes | fake | Replace with real health data or remove | frontend |
| ML Brain & Flywheel | KPI/model/log fallbacks | Uses fallback empty objects/arrays | Real ML telemetry | `/api/v1/flywheel/*`, `/api/v1/ml-brain/*` | partial | Show truthful empty states rather than merged fallbacks | frontend |
| Patterns | Pattern/screener panels | Mostly real fetches but thin zero-state semantics | Truthful screener/pattern console | `/api/v1/patterns`, `/api/v1/signals/` | partial | Add no-pattern/error/disconnect states | frontend |
| Backtesting | Run history / strategy universe | Needs backend route audit for static examples and hardcoded selectors | Real backtesting lab | `/api/v1/backtest/*` | partial / needs audit | Remove any static run/strategy lists and use backend sources only | both |
| Performance | KPI/trade/attribution fallbacks | Merges fallback structures into real data | Real portfolio analytics | `/api/v1/performance`, `/api/v1/performance/trades`, `/api/v1/agents/consensus`, `/api/v1/risk/risk-score`, `/api/v1/flywheel` | partial | Remove fallback numbers, add no-history state | frontend |
| Market Regime | Regime parameters | Has default regime parameter fallback object | Truthful regime dashboard | `/api/v1/openclaw/*`, `/api/v1/strategy/regime-params`, `/api/v1/risk/risk-score` | partial | Render unavailable/config-missing state instead of defaults | frontend |
| Trades | Open positions/orders | Real broker polling but limited freshness/disconnect treatment | Live operator trade board | `/api/v1/alpaca/account`, `/positions`, `/orders`, `/api/v1/portfolio` | real / partial | Add freshness/disconnect and admin controls | frontend |
| Risk Intelligence | Safety checks / gauges | Some defaults look optimistic when data is missing | Truthful risk console | `/api/v1/risk*`, `/api/v1/risk-shield*`, `/api/v1/portfolio` | partial | Replace PASS-like defaults with unknown/unavailable | frontend |
| Trade Execution | Strategy/options/news chrome | Mixed real quote/order data with static presentation risk | Real order-entry console | `/api/v1/orders*`, `/api/v1/alpaca*`, `/api/v1/quotes*`, `/api/v1/alignment*`, `/api/v1/council*`, `/api/v1/stocks/` | partial | Remove hardcoded strategy/news/options placeholder surfaces | frontend |
| Settings | Connection badges | Several badges imply live status/config validity without direct validation | Truthful system configuration surface | `/api/v1/settings*`, `/api/v1/system`, `/api/v1/system/device`, auth endpoints | partial / stale | Bind every badge to a real validation or restyle as neutral config state | frontend |
| Backend logs route | Logs data | Backend still returns fixed sample log list | Operator log stream | `backend/app/api/v1/logs.py` | stale | Replace backend stub with real log pipeline | backend |
| Backend drift route | Drift metrics | Backend fallback still returns mock-structure metrics when monitor unavailable | Drift diagnostics | `backend/app/api/v1/agents.py` (`/drift`) | stale | Return unavailable state instead of fabricated metric rows | backend |
